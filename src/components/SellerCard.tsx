
// This component has been intentionally emptied as part of removing marketplace features
// The original SellerCard component was used for displaying seller information in the marketplace
// Since marketplace functionality is being removed, this component is no longer needed

const SellerCard = () => {
  return null;
};

export default SellerCard;
